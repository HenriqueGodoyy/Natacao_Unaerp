{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node -e \"const fs=require('fs'),os=require('os'),path=require('path');const r=fs.readFileSync('README.md','utf8');fs.appendFileSync(path.join(os.tmpdir(),'natacao_hook_test.log'), new Date().toISOString()+' SessionStart hook rodou\\n');process.stdout.write(JSON.stringify({systemMessage:'Hook SessionStart OK: README.md carregado no contexto ('+r.length+' chars).',hookSpecificOutput:{hookEventName:'SessionStart',additionalContext:'Conteudo do README.md do projeto (leia para ter contexto):\\n\\n'+r}}))\"",
            "statusMessage": "Lendo README.md do projeto..."
          }
        ]
      }
    ]
  }
}
